# -----------------------------------------------------------------------------
# Optional settings for ./build.sh — copy to build.env and edit.
# File build.env is gitignored and is not pushed to GitHub.
#
# CLI overrides (highest precedence): --docker-repo, --platform
#
# Documentation: README.md section "Docker Hub builds".
# -----------------------------------------------------------------------------

# Docker Hub repository (namespace/name, NO tag). Used for image tags and pushrm.
DOCKER_IMAGE_REPO=company/nzbgetvpn

# Passed to: docker buildx build --platform ...
BUILD_PLATFORM=linux/amd64,linux/arm64
